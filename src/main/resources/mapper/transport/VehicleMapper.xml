<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.schoolbus.transport.infrastructure.persistence.vehicle.VehicleMapper">

    <resultMap
            id="VehicleResultMap"
            type="com.schoolbus.transport.infrastructure.persistence.vehicle.VehicleDataObject">
        <id property="id" column="id"/>
        <result property="vehicleNumber" column="vehicle_no"/>
        <result property="licensePlate" column="license_plate"/>
        <result property="seatCount" column="seat_count"/>
        <result property="status" column="status"/>
        <result property="version" column="version"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert
            id="insertVehicle"
            parameterType="com.schoolbus.transport.infrastructure.persistence.vehicle.VehicleDataObject"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO transport_vehicle (
            vehicle_no,
            license_plate,
            seat_count,
            status,
            version,
            created_at,
            updated_at
        ) VALUES (
            #{vehicleNumber},
            #{licensePlate},
            #{seatCount},
            #{status},
            #{version},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <update id="updateWithVersion">
        UPDATE transport_vehicle
        SET status = #{vehicle.status},
            version = #{vehicle.version},
            updated_at = #{vehicle.updatedAt}
        WHERE id = #{vehicle.id}
          AND version = #{expectedVersion}
    </update>

    <select id="selectById" resultMap="VehicleResultMap">
        SELECT
            id,
            vehicle_no,
            license_plate,
            seat_count,
            status,
            version,
            created_at,
            updated_at
        FROM transport_vehicle
        WHERE id = #{id}
    </select>

    <select id="selectByVehicleNumber" resultMap="VehicleResultMap">
        SELECT
            id,
            vehicle_no,
            license_plate,
            seat_count,
            status,
            version,
            created_at,
            updated_at
        FROM transport_vehicle
        WHERE vehicle_no = #{vehicleNumber}
    </select>

    <select id="selectByLicensePlate" resultMap="VehicleResultMap">
        SELECT
            id,
            vehicle_no,
            license_plate,
            seat_count,
            status,
            version,
            created_at,
            updated_at
        FROM transport_vehicle
        WHERE license_plate = #{licensePlate}
    </select>

    <select id="selectAll" resultMap="VehicleResultMap">
        SELECT
            id,
            vehicle_no,
            license_plate,
            seat_count,
            status,
            version,
            created_at,
            updated_at
        FROM transport_vehicle
        <where>
            <if test="status != null">
                status = #{status}
            </if>
        </where>
        ORDER BY id
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*)
        FROM transport_vehicle
        <where>
            <if test="status != null">
                status = #{status}
            </if>
        </where>
    </select>

</mapper>
